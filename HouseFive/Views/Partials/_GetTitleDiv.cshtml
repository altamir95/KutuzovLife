@model List<string>

<div class="title-div d-flex align-items-center mb-4 " style="background-image: url(@Model[0])">
    <div class="container-fluid myImg">
        <div class="row justify-content-center">
            <div class="col-11 col-sm-10 col-md-9 col-lg-8 col-xl-8 py-5">
                <p class="text-center text-white display-6 text-uppercase   fw-bold">@Model[1]</p>
            </div>
        </div>
    </div>
</div>